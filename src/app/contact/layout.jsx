import React from 'react'
// import styles from './page.module.css'

export const metadata = {
    title: 'Contact Us',
    description: 'Reach us immediately through this medium',
}

const layout = ({children}) => {
  return (
    <div>
        {children}
    </div>
  )
}

export default layout